[package]
name = "minios-esp-rs"
version = "2.0.4"
edition = "2021"
description = "Rust rewrite of the MiniOS-ESP shell and core services"
license = "MIT"

[lib]
path = "rust/src/lib.rs"

[[bin]]
name = "minios"
path = "rust/src/main.rs"

[profile.release]
lto = true
codegen-units = 1
strip = true
